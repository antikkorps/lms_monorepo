version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    target-branch: "dev"
    open-pull-requests-limit: 10
    # 90-day cooldown before non-security upgrades.
    # Security updates bypass this entirely (separate Dependabot security flow).
    cooldown:
      default-days: 90
    groups:
      nx:
        patterns:
          - "@nx/*"
          - "nx"
      vue:
        patterns:
          - "vue*"
          - "@vue/*"
          - "@vitejs/plugin-vue"
      testing:
        patterns:
          - "vitest"
          - "@vitest/*"
      linting:
        patterns:
          - "eslint*"
          - "typescript-eslint"
          - "prettier"
    labels:
      - "dependencies"
      - "automated"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    target-branch: "dev"
    labels:
      - "dependencies"
      - "ci"
